services:
  - type: web_service
    name: mind-a-thought-backend
    env: docker
    plan: free
    region: oregon
    branch: main
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
    autoDeploy: true
